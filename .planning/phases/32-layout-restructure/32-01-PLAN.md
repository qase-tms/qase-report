---
phase: 32-layout-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/tabs.tsx
  - src/components/TabNavigation/index.tsx
autonomous: true

must_haves:
  truths:
    - "User sees 5 horizontal tabs for navigation"
    - "Active tab visually highlighted"
    - "Clicking tab changes active view"
  artifacts:
    - path: "src/components/ui/tabs.tsx"
      provides: "shadcn/ui Tabs component"
      exports: ["Tabs", "TabsList", "TabsTrigger", "TabsContent"]
    - path: "src/components/TabNavigation/index.tsx"
      provides: "Tab navigation with 5 tabs"
      exports: ["TabNavigation"]
  key_links:
    - from: "src/components/TabNavigation/index.tsx"
      to: "useRootStore().activeView"
      via: "controlled Tabs value/onValueChange"
      pattern: "value=\\{activeView\\}"
---

<objective>
Install shadcn/ui Tabs component and create TabNavigation component with 5 tabs.

Purpose: Replace hamburger menu with horizontal tab navigation for better discoverability.
Output: TabNavigation component ready for layout integration.
</objective>

<execution_context>
@/Users/gda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-layout-restructure/32-RESEARCH.md

@src/store/index.tsx
@src/components/ui/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn/ui Tabs component</name>
  <files>src/components/ui/tabs.tsx</files>
  <action>
Install Tabs component via shadcn CLI:
```bash
npx shadcn@latest add tabs -y
```

Verify the generated file exists at src/components/ui/tabs.tsx with exports: Tabs, TabsList, TabsTrigger, TabsContent.

The component uses @radix-ui/react-tabs (already installed as dependency of radix-ui@1.4.3).
  </action>
  <verify>
```bash
cat src/components/ui/tabs.tsx | grep -E "^export"
```
Should show exports for Tabs, TabsList, TabsTrigger, TabsContent.
  </verify>
  <done>src/components/ui/tabs.tsx exists with all four exports</done>
</task>

<task type="auto">
  <name>Task 2: Create TabNavigation component with 5 tabs</name>
  <files>src/components/TabNavigation/index.tsx</files>
  <action>
Create TabNavigation component that:
1. Uses controlled Tabs with MobX activeView state
2. Renders 5 tabs with icons:
   - tests (List icon) - "Test cases"
   - dashboard (LayoutDashboard icon) - "Overview"
   - failure-clusters (AlertCircle icon) - "Failure Clusters"
   - gallery (Images icon) - "Gallery"
   - comparison (ArrowLeftRight icon) - "Comparison"
3. Uses observer() for MobX reactivity
4. Custom TabsList styling: full width, left-aligned, border-bottom, no background
5. TabsTrigger styling: border-bottom-2 for active state (not pill style)
6. Icons from lucide-react (already imported in App.tsx)
7. Responsive: show icons only on mobile (hidden sm:inline for labels)

Note: Do NOT use TabsContent - content rendered separately in MainLayout to preserve react-window virtual scrolling.

Reference research pattern from 32-RESEARCH.md "Pattern 2: Controlled Tabs with MobX State".
  </action>
  <verify>
```bash
cat src/components/TabNavigation/index.tsx | head -50
```
Should show observer() wrapper, controlled Tabs with value={activeView}, 5 TabsTriggers with icons.
  </verify>
  <done>TabNavigation component renders 5 tabs connected to MobX activeView</done>
</task>

</tasks>

<verification>
1. `npm run build` - TypeScript compilation passes
2. `npm run dev` - App loads without errors
3. TabNavigation component can be imported: `import { TabNavigation } from './components/TabNavigation'`
</verification>

<success_criteria>
- shadcn/ui Tabs installed at src/components/ui/tabs.tsx
- TabNavigation component created with 5 tabs
- Tabs use MobX activeView for controlled state
- TypeScript types correct (no any types)
</success_criteria>

<output>
After completion, create `.planning/phases/32-layout-restructure/32-01-SUMMARY.md`
</output>
