---
phase: 31-core-ui-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/card.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/skeleton.tsx
  - package.json
autonomous: true

must_haves:
  truths:
    - "Card component renders with dark theme background"
    - "Badge component displays text with rounded styling"
    - "Skeleton component shows animated placeholder"
    - "All components TypeScript compile without errors"
  artifacts:
    - path: "src/components/ui/card.tsx"
      provides: "Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter exports"
      contains: "CardTitle"
    - path: "src/components/ui/badge.tsx"
      provides: "Badge component with variant prop"
      contains: "badgeVariants"
    - path: "src/components/ui/skeleton.tsx"
      provides: "Skeleton component with animate-pulse"
      contains: "Skeleton"
  key_links:
    - from: "src/components/ui/card.tsx"
      to: "@/lib/utils"
      via: "cn utility import"
      pattern: "import.*cn.*from.*@/lib/utils"
    - from: "src/components/ui/badge.tsx"
      to: "@/lib/utils"
      via: "cn utility import"
      pattern: "import.*cn.*from.*@/lib/utils"
---

<objective>
Install core shadcn/ui components (Card, Badge, Skeleton) via CLI.

Purpose: Provide foundational UI components for test report interface.
Output: Three CLI-generated component files in src/components/ui/ ready for use.
</objective>

<execution_context>
@/Users/gda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-core-ui-components/31-RESEARCH.md
@src/components/ui/button.tsx
@src/lib/utils.ts
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Card, Badge, and Skeleton components via CLI</name>
  <files>
    src/components/ui/card.tsx
    src/components/ui/badge.tsx
    src/components/ui/skeleton.tsx
    package.json
  </files>
  <action>
Run shadcn CLI to install all three components in a single command:

```bash
npx shadcn@latest add card badge skeleton -y
```

The `-y` flag skips confirmation prompts. CLI will:
1. Create src/components/ui/card.tsx with Card subcomponents
2. Create src/components/ui/badge.tsx with CVA variants
3. Create src/components/ui/skeleton.tsx with animate-pulse styling
4. Install any missing peer dependencies to package.json

Verify the files are created with correct imports:
- All should import `cn` from `@/lib/utils`
- Badge should use `cva` from `class-variance-authority`
- Card should export CardHeader, CardTitle, CardDescription, CardContent, CardFooter
  </action>
  <verify>
```bash
ls -la src/components/ui/
cat src/components/ui/card.tsx | head -30
cat src/components/ui/badge.tsx | head -30
cat src/components/ui/skeleton.tsx
```
Verify all three files exist and have correct imports.
  </verify>
  <done>
card.tsx, badge.tsx, skeleton.tsx exist in src/components/ui/ with proper cn() utility imports.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify TypeScript compilation and component exports</name>
  <files>
    src/components/ui/card.tsx
    src/components/ui/badge.tsx
    src/components/ui/skeleton.tsx
  </files>
  <action>
Run TypeScript compiler to verify all components compile without errors:

```bash
npx tsc --noEmit
```

If any TypeScript errors occur, fix them. Common issues:
- Missing type imports (add `import type { ... }`)
- Slot.Root type casting (already handled in button.tsx pattern)

Verify component exports are correctly typed:
- Card exports: Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter, CardAction
- Badge exports: Badge, badgeVariants (with VariantProps)
- Skeleton exports: Skeleton

Check that all components use the cn() utility from @/lib/utils for className merging.
  </action>
  <verify>
```bash
npx tsc --noEmit && echo "TypeScript compilation successful"
```
No TypeScript errors reported.
  </verify>
  <done>
All three components pass TypeScript compilation with explicit type exports.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify build succeeds with new components</name>
  <files>None (build verification only)</files>
  <action>
Run full Vite build to ensure new components don't break the production build:

```bash
npm run build
```

The build should complete without errors. This verifies:
1. All imports resolve correctly
2. Tailwind CSS processes component classes
3. No circular dependency issues
4. Tree-shaking works (unused exports don't bloat bundle)

If build fails, check for:
- Missing dependencies in package.json
- Import path issues (@/ alias)
- CSS class conflicts
  </action>
  <verify>
```bash
npm run build
```
Build completes with "built in Xs" message, no errors.
  </verify>
  <done>
Production build succeeds with all three new components included.
  </done>
</task>

</tasks>

<verification>
1. `ls src/components/ui/` shows card.tsx, badge.tsx, skeleton.tsx alongside existing button.tsx, alert.tsx
2. `npx tsc --noEmit` passes with no errors
3. `npm run build` completes successfully
4. All components import cn from @/lib/utils
</verification>

<success_criteria>
- Card, Badge, Skeleton components installed via CLI
- TypeScript compiles without errors
- Production build succeeds
- Components ready for use in application code
</success_criteria>

<output>
After completion, create `.planning/phases/31-core-ui-components/31-01-SUMMARY.md`
</output>
