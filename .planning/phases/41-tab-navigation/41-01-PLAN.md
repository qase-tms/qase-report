---
phase: 41-tab-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/TabNavigation/index.tsx
  - src/store/index.tsx
autonomous: true

must_haves:
  truths:
    - "User lands on Test cases tab when opening the report"
    - "Tab order shows Test cases first in the tab bar"
    - "User sees 'Analytics' label instead of 'Overview' in tab navigation"
    - "Analytics tab content remains the same (only label changes)"
  artifacts:
    - path: "src/components/TabNavigation/index.tsx"
      provides: "Tab navigation with reordered tabs and Analytics label"
      contains: "label: 'Analytics'"
    - path: "src/store/index.tsx"
      provides: "Default activeView state set to tests"
      contains: "activeView: 'dashboard' | 'tests'"
  key_links:
    - from: "src/store/index.tsx"
      to: "src/components/TabNavigation/index.tsx"
      via: "activeView state consumed by TabNavigation"
      pattern: "activeView.*=.*'tests'"
---

<objective>
Reorder tabs to show Test cases first and rename Overview tab to Analytics.

Purpose: Users expect to see their test results immediately when opening a report. The Overview tab (now Analytics) provides supplementary analysis, not the primary view.

Output: Updated TabNavigation component with new tab order and label, updated store with new default view.
</objective>

<execution_context>
@/Users/gda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/TabNavigation/index.tsx
@src/store/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update default activeView to 'tests' in RootStore</name>
  <files>src/store/index.tsx</files>
  <action>
Change the default value of `activeView` from `'dashboard'` to `'tests'` on line 23:

```typescript
// Before:
activeView: 'dashboard' | 'tests' | 'analytics' | 'failure-clusters' | 'gallery' | 'comparison' | 'timeline' = 'dashboard'

// After:
activeView: 'dashboard' | 'tests' | 'analytics' | 'failure-clusters' | 'gallery' | 'comparison' | 'timeline' = 'tests'
```

This single-line change ensures users land on Test cases view by default when opening the report.
  </action>
  <verify>
Run `grep "activeView.*=.*'tests'" src/store/index.tsx` - should match the default assignment.
Run `npm run build` - should complete without TypeScript errors.
  </verify>
  <done>Default activeView is 'tests', build passes, no type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Reorder tabs and rename Overview to Analytics</name>
  <files>src/components/TabNavigation/index.tsx</files>
  <action>
Update the `tabs` array to:
1. Move 'tests' tab to the first position
2. Rename 'Overview' to 'Analytics' (keep the value as 'dashboard' for now to maintain routing compatibility)

```typescript
const tabs = [
  { value: 'tests', label: 'Test cases', icon: List },
  { value: 'dashboard', label: 'Analytics', icon: LayoutDashboard },
  {
    value: 'failure-clusters',
    label: 'Failure Clusters',
    icon: AlertCircle,
  },
  { value: 'gallery', label: 'Gallery', icon: Images },
  { value: 'comparison', label: 'Comparison', icon: ArrowLeftRight },
  { value: 'timeline', label: 'Timeline', icon: Clock },
] as const
```

Note: Keep `value: 'dashboard'` for the Analytics tab to maintain compatibility with existing routing and MainLayout view switching. The label change is purely cosmetic.
  </action>
  <verify>
Run `npm run dev` and visually confirm:
1. Test cases tab appears first in the tab bar
2. Second tab shows "Analytics" label (not "Overview")
3. Clicking Analytics tab still shows the dashboard/overview content
  </verify>
  <done>Tabs display in new order (Test cases first), Overview renamed to Analytics, all tab navigation works correctly.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npm run dev` shows Test cases tab first
3. Opening the app lands on Test cases view (not dashboard)
4. Analytics tab label visible, clicking it shows dashboard content
5. All other tabs (Failure Clusters, Gallery, Comparison, Timeline) still work
</verification>

<success_criteria>
- User opens report and immediately sees Test cases tab selected
- Tab order: Test cases, Analytics, Failure Clusters, Gallery, Comparison, Timeline
- "Overview" label no longer visible anywhere in tab navigation
- Analytics tab content identical to previous Overview content
- No TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/41-tab-navigation/41-01-SUMMARY.md`
</output>
